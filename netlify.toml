[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--version" # Just to check version in logs

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
